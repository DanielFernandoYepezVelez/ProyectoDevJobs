<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>DevJobs Project</title>
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/app.css">
</head>

<body>

  <div class="nombre-sitio contenedor">
    <h1>
      <a href="/">{{nombrePagina}}</a>
    </h1>

    {{#if barra}}
    <div class="buscador">
      <form action="#">
        <input type="text" name="#" id="#" class="buscar">
        <input type="submit" value="Buscar">
      </form>
    </div>
    {{/if}}
  </div>

  <div class="site-header contenedor separador">
    <h2>{{nombrePagina}}</h2>

    {{#if tagline}}
    <p class="tagline">{{tagline}}</p>
    {{/if}}

    {{#if boton}}
    <a href="/vacante/nueva" class="btn btn-azul">Publicar Nueva Vacante</a>
    {{/if}}
  </div>

  <div class="contenido-principal contenedor">
    {{{ body }}}
  </div>

</body>

</html>